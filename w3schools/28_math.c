#include <stdio.h>
#include <math.h>

int main() {
	
  printf("%f\n", sqrt(16));
  printf("%f\n", ceil(1.4));  // rounds upwards to nearest integer
  printf("%f\n", floor(1.4)); // rounds downwards to nearest integer
  printf("%f\n", pow(4, 3));  // x to the power of y
  
  return 0;
}